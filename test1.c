{
	int a, b = 9;
	int A[10][10];
	char sum;
	int u, count = 0;
	count = count / (u*b);
	switch(a){
		case 1:
			if(b != 0 && a < b){
				a = a + b;
			}
			else if(a == b || !(a)){
				a = -a + b++;
			}
			else{
				a = a - b;
			}
			break;
		case sum:
			while(sum <= 1){
				float f = 0.3;
				a = a*sum + 4;
			}
			break;

		case b*2:
			b = f*2;	
		default:
			--sum;
	}
}
